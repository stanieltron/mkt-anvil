{
  "name": "mkt-anvil",
  "version": "1.0.0",
  "private": true,
  "description": "Railway-friendly Anvil local chain service for Makeit",
  "scripts": {
    "install:foundry": "node scripts/install-foundry.js",
    "prestart": "npm run install:foundry",
    "start": "node scripts/start-chain.js",
    "local:chain": "node scripts/run-local-chain.js",
    "local:chain:fresh": "node scripts/run-local-chain.js --fresh"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "@openzeppelin/contracts-upgradeable": "^5.6.1",
    "ethers": "^6.16.0"
  }
}
